<!DOCTYPE html>
<html lang="en">
    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <meta charset="UTF-8" />
        <title>{% block title %}Bilemo -{% endblock %}</title>
        <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous" />
        <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">
        <link rel="stylesheet" href="https://use.typekit.net/sec3dre.css">
        <link rel="stylesheet" href="css/style.css">
        <link rel="icon" type="image/x-icon" href="{{ asset('img/favicon.ico') }}" />
    </head>
    <body>
        <nav class="navbar navbar-expand-sm fixed-top" id="nav_container">
            <a id="logo" href="{{ path('bi_login') }}"><img src="{{ asset('img/logo.png') }}" alt="bilemo logo"/></a>
            <ul class="nav">
                {% if not is_granted('IS_AUTHENTICATED_FULLY')%}
                    <li class="nav-item">
                        <a id="login" class="nav-link text-light" href="{{ path('bi_login') }}">Log in</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a id="space" class="nav-link text-light" href="{{ path('bi_space') }}">My space</a>
                    </li>
                    <li class="nav-item">
                        <a id="logout" class="nav-link text-light" href="{{ path('bi_logout') }}">Logout</a>
                    </li>
                {% endif %}
            </ul>
        </nav>
        {% block body %}{% endblock %}
        <footer class="page-footer">
            <div id="copyright" class="footer-copyright text-left py-4 pl-3 bg-dark text-light">Copyright @ 2019</div>
        </footer>
        {% block javascripts %}
            <script>
                (function() {
                    var newURL = location.href.split("?")[0];
                    newURL = newURL.split("#")[0];
                    window.history.pushState('object', document.title, newURL);
                })();
            </script>
        {% endblock %}
    </body>
</html>